{
  "name": "function-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "run-s build serve",
    "build": "run-s clean && run-p build:*",
    "build:html": "mkdirp dist && cpr index.html dist/",
    "build:lambda": "mkdirp lambda && cpr my-function.js lambda/",
    "clean": "rimraf dist lambda",
    "serve": "http-server dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpr": "^3.0.1",
    "http-server": "^0.10.0",
    "mkdirp": "^0.5.1",
    "npm-run-all": "^4.1.2",
    "rimraf": "^2.6.2"
  }
}
